# RepoGuard

Work in this repo as a single agent in the main chat: investigate, implement, and verify here. Do not split routine work across coordinator / worker / reviewer subagents unless the user explicitly asks.
