{
  "name": "repoguard",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:agent": "npm --prefix agent test",
    "agent": "npm --prefix agent start"
  }
}


